{
  "name": "neurostep",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "deploy:indexes": "firebase deploy --only firestore:indexes"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "husky": "^9.1.7",
    "prettier": "^3.7.4"
  }
}
