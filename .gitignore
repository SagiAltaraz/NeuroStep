# Environment files with real secrets — never commit these
backend/.env
backend/game-server/.env
.env.local
.env.*.local

# macOS
.DS_Store

# Dependencies
node_modules/

# Build outputs
dist/
build/
